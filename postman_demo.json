{
  "info": {
    "_postman_id": "c1a4e9b2-1111-2222-3333-vulnapi12345",
    "name": "Deliberately Vulnerable Test API",
    "description": "Intentionally vulnerable API for security scanner testing",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [

    {
      "name": "Login Endpoint",
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"username\": \"admin\",\n  \"password\": \"password123\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/login",
          "host": ["{{base_url}}"],
          "path": ["login"]
        }
      }
    },

    {
      "name": "Get User By ID",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/users/1",
          "host": ["{{base_url}}"],
          "path": ["users", "1"]
        }
      }
    },

    {
      "name": "Admin Dashboard",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/admin/dashboard",
          "host": ["{{base_url}}"],
          "path": ["admin", "dashboard"]
        }
      }
    },

    {
      "name": "Search Endpoint",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/search?q=test",
          "host": ["{{base_url}}"],
          "path": ["search"],
          "query": [
            {
              "key": "q",
              "value": "test"
            }
          ]
        }
      }
    },

    {
      "name": "Ping Host",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/ping?host=127.0.0.1",
          "host": ["{{base_url}}"],
          "path": ["ping"],
          "query": [
            {
              "key": "host",
              "value": "127.0.0.1"
            }
          ]
        }
      }
    },

    {
      "name": "Download File",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{base_url}}/download?file=report.txt",
          "host": ["{{base_url}}"],
          "path": ["download"],
          "query": [
            {
              "key": "file",
              "value": "report.txt"
            }
          ]
        }
      }
    }

  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:3000"
    }
  ]
}